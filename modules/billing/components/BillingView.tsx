"use client"

/**
 * Billing view — single entry point for finance invoices.
 * Re-exports the canonical invoice UI (listing, detail, KPIs, filters, creation, payments).
 */
export { InvoiceView as BillingView } from "@/modules/invoicing/components"
